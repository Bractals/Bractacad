import * as THREE from 'three';

const ray = new THREE.Raycaster();

export default ray;

// ray.params.Points.threshold = 0.1; // Set a threshold for point intersection
// ray.params.Line.threshold = 0.1; // Set a threshold for line intersection
// ray.params.Mesh.threshold = 0.1; // Set a threshold for mesh intersection
// ray.params.Sprite.threshold = 0.1; // Set a threshold for sprite intersection
